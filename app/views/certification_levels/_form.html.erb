<%= form_for(@certification_level) do |f| %>
  <% if @certification_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@certification_level.errors.count, "error") %> prohibited this certification_level from being saved:</h2>

      <ul>
      <% @certification_level.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
