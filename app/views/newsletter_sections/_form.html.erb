<%= form_for([@newsletter,@newsletter_section], :html => {:multipart => true}) do |f| %>
  <% if @newsletter_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@newsletter_section.errors.count, "error") %> prohibited this newsletter_section from being saved:</h2>

      <ul>
      <% @newsletter_section.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
